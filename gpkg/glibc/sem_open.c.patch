--- src/sysdeps/pthread/sem_open.c	2026-01-23 23:54:00.000000000 +0300
+++ src/sysdeps/pthread/sem_open.c.patch	2026-07-07 19:30:31.802174074 +0300
@@ -150,7 +150,7 @@
 					 fd, 0)) != MAP_FAILED)
 	{
 	  /* Create the file.  Don't overwrite an existing file.  */
-	  if (__link (tmpfname, dirname.name) != 0)
+	  if (__symlink (tmpfname, dirname.name) != 0)
 	    {
 	      /* Undo the mapping.  */
 	      __munmap (result, sizeof (sem_t));
